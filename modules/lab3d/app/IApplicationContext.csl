import lab3d.core.scene.IScene;
import lab3d.app.manipulator.IManipulatorManager;

interface IApplicationContext
{
	// Creates a whole new context.
	void blank();
	readonly IScene currentScene;
	readonly IManipulatorManager manipulatorManager;
};