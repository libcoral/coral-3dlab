import lab3d.app.manipulator.IManipulator;

interface IManipulatorManager
{
	void setCurrent( in string name );
	IManipulator getManipulatorByName( in string name );
	void addManipulator( in IManipulator manipulator );
};
