interface INavigator
{
	void evolve( in double dt );

	/*!
		Evolves navigation timeline by \a dt. After a call to evolve, the view will be closer to (or at) the final desired position and orientation.
	 */
	lab3d.dom.IView view;
};
