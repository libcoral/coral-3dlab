import coOsg.NodePtr;

/*!
	A service representing a model that can provide OpenSceneGraph geometry.
*/
interface IModel
{	
	string filename;
	coOsg.NodePtr getNode();
	lab3d.dom.BoundingBox getBounds();
};

