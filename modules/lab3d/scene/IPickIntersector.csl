//! Interface for picking algorithm implementations.
interface IPickIntersector
{
	/*! 
		Performs an intersection based on screen (x,y) coordinates.
		\param x screen horizontal coordinate 
		\param y screen vertical coordinate 
		\param intersections list of intersected objects
	 */
	void intersect( in double x, in double y, out PickIntersection[] intersections );
};
